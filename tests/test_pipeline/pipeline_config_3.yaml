pipeline:
  # unique names
  # should be a list to ensure ordering
  StandardScaler1:
    # name of the transformer class
    class : 'StandardScaler'
    input_col : ['c']
    copy : True
    with_mean : True
